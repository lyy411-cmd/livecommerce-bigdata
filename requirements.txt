# 星播大数据分析平台 - Python 依赖
# 安装: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 核心依赖
pymysql>=1.0.0
requests>=2.28.0

# Kafka
kafka-python>=2.0.2

# WebSocket
websockets>=12.0

# 浏览器自动化 (爬虫)
playwright>=1.40.0
playwright-stealth>=1.0.6

# 大数据组件 (可选)
pyhive[hive]>=0.7.0
thrift>=0.16.0

# 数据可视化 (可选)
pandas>=2.0.0

# protobuf (可选，已内置解码器)
# protobuf>=4.25.0
